package ibis.constellation;

import java.io.Serializable;

/**
 * A <code>ConstellationIdentifier</code> identifies a constellation.
 *
 * TODO: remove? There are no methods that require a constellation identifier in
 * the API.
 */
public abstract class ConstellationIdentifier implements Serializable {
}
